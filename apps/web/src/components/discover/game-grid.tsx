import type { DiscoverGame } from "@/lib/discover/types";
import { DiscoverGameCard } from "@/components/discover/game-card";
type GameGridProps = { games: readonly DiscoverGame[]; onPreview: (game: DiscoverGame) => void };
export function GameGrid({ games, onPreview }: GameGridProps) { return <section aria-labelledby="games-title"><div className="flex items-end justify-between gap-4"><div><p className="eyebrow">Em circulação</p><h2 id="games-title" className="display mt-3 text-4xl leading-none">Jogos para considerar</h2></div><p className="text-sm text-(--ink-muted)">{games.length} encontrados</p></div>{games.length ? <div className="mt-7 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{games.map((game) => <DiscoverGameCard key={game.id} game={game} onPreview={onPreview} />)}</div> : <div className="mt-7 rounded-2xl border border-dashed border-(--line) p-8"><p className="display text-3xl">Nada por aqui ainda.</p><p className="mt-3 text-sm text-(--ink-muted)">Tente outro título, gênero ou filtro.</p></div>}</section>; }
